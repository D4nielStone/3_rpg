services:
  - type: web
    name: webgl-rpg-multiplayer
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm run multiplayer
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: DATABASE_URL
        sync: false